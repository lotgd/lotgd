name: Unit test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:10.6
        ports:
          - '3306:3306'
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
          MARIADB_ROOT_PASSWORD: lotgd
          MYSQL_DATABASE: lotgd_test
        options: >-
          --health-cmd "healthcheck.sh --connect --innodb_initialized || mysqladmin ping --protocol tcp --silent"

    steps:
      - name: Install PHP version
        uses: shivammathur/setup-php@2cb9b829437ee246e9b3cac53555a39208ca6d28
        with:
          php-version: '8.4'

      - name: Use Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          
      - uses: actions/checkout@v4
      
      - name: Copy .env.test.local
        run: php -r "file_exists('.env.test.local') || copy('.env.test', '.env.test.local');"
  
      - name: Validate composer.json and composer.lock
        run: composer validate --strict

      - name: Cache Composer packages
        id: composer-cache
        uses: actions/cache@v3
        with:
          path: vendor
          key: ${{ runner.os }}-php-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-php-
        
      - name: Install PHP dependencies
        run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

      - name: Run install
        uses: borales/actions-yarn@v4
        with:
          cmd: install

      - name: Build webpack
        uses: borales/actions-yarn@v4
        with:
          cmd: run build

      - name: Setup database
        env:
          DATABASE_URL: mysql://root:lotgd@127.0.0.1:3306/lotgd?serverVersion=10.6.24-MariaDB&charset=utf8mb4
        run: |
          bin/console doctrine:migrations:migrate first --quiet --no-interaction -e test
          bin/console doctrine:migrations:migrate --quiet --no-interaction -e test
          bin/console doctrine:fixtures:load --quiet --no-interaction -e test

      - name: Run test suite
        env:
          DATABASE_URL: mysql://root:lotgd@127.0.0.1:3306/lotgd?serverVersion=10.6.24-MariaDB&charset=utf8mb4
        run: vendor/bin/phpunit --stop-on-failure
      
